{
  "name": "ai-assisted-image-editing-for-ps",
  "version": "0.2.4-alpha",
  "description": "For using $300 from Google and 400 free image from Bytedance",
  "author": "dw",
  "license": "GPL-3.0",
  "scripts": {
    "test": "NODE_OPTIONS=--require=./tests/setup.js node ./tests/run-tests.js"
  }
}
